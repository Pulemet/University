@model University.Models.Dto.DialogToUsersDto

<tr class="dialog-container">
    <td>
        <a href="@Url.Action("DialogPage", "Home", new {id = Model.Id})">@Model.Members[0].SurName @Model.Members[0].FirstName</a>
    </td>
</tr>